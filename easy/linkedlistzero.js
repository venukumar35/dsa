class LinkedListOfzero {
  constructor(arr) {
    this.arr = arr;
  }
  zero() {
    let arr = this.arr.sort((a, b) => {
      return a - b;
    });
    for (let i = 0; i < arr.length; i++) {
      if (head == null) {
        node;
      }
    }
  }
}
const ObjectForLinkedListOfzero = new LinkedListOfzero([
  1, 2, 2, 1, 2, 0, 2, 2,
]);
console.log(ObjectForLinkedListOfzero.zero());

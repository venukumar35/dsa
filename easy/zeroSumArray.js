class SolutionfindSubarray {
  //Function to count subarrays with sum equal to 0.
  findSubarray(arr, n) {
    for (let i = 0; i < arr.length; i++) {
      for (let j = 0; j < arr.length; j++) {}
    }
  }
}
const arrfindSubarray = [0, 0, 5, 5, 0, 0];

const dataObj = new SolutionfindSubarray();
console.log(dataObj.findSubarray(arrfindSubarray));
/**The 6 subarrays are 
[0], [0], [0], [0], [0,0], and [0,0]. */
